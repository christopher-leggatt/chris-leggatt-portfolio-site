import HomeContent from "./components/HomeContent";

export const metadata = {
  title: "Home",
};

const Home = () => {
  return (
    <div>
      <HomeContent />
    </div>
  );
};

export default Home;
