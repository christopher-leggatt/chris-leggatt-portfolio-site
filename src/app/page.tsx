import HomeContent from "@/app/(components)/HomeContent/HomeContent";

export const metadata = {
  title: "Home",
};

const Home = () => {

  return (
    <div>
      <HomeContent />
    </div>
  );
};

export default Home;
